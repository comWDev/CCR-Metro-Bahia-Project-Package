@inherits Umbraco.Web.Mvc.UmbracoTemplatePage
@inherits Umbraco.Web.Mvc.UmbracoTemplatePage
@using Archetype.Models;
@using Archetype.Extensions;

@{
    Layout = null;
}

@foreach( var menuData in 
    Model.Content
    .AncestorOrSelf("home")
    .GetPropertyValue<IPublishedContent>("infoNumber")
    .GetPropertyValue<ArchetypeModel>("infoAtype"))

{}
